using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cour : MonoBehaviour
{
    // Start is called before the first frame update

    /*IEnumerator <имя корутина>() //необходимо вызывать из функций событий
     * yield return new WaitForSeconds(<Количество секунд задержки до следующего кода>1.5f);
     * Instantiate();
     */
    void Start()
    {
        //Invoke("<название функции которую нужно выполнить>, задержка по времени в секундах");
        //if(GetKey(KeyCode.F))
        //{
        //  Start Coroutine(<имя корутина>());
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
